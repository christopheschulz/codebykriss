---
const stats = [
  { number: 'React Native', label: 'Spécialité' },
  { number: 'IA-Powered', label: 'Développement' },
  { number: 'Sur-mesure', label: 'Solutions' },
];
---

<section id="accueil" class="hero min-h-screen flex flex-col items-center justify-center bg-gradient-to-br from-primary-600 to-primary-800 text-white py-24 pb-16 relative overflow-hidden">
  <div class="hero-bg absolute inset-0 bg-[url('https://images.unsplash.com/photo-1451187580459-43490279c0fa?ixlib=rb-4.0.3')] bg-center bg-cover opacity-10 z-0"></div>

  <div class="hero-content relative z-10 text-center max-w-[900px] w-full px-4">
    <div class="hero-text">
      <div class="hero-badge inline-flex items-center gap-2 py-2 px-4 bg-white/15 border border-white/20 rounded-full text-sm font-medium mb-6 backdrop-blur-lg">
        <span class="badge-icon text-[1.1em]">✨</span>
        <span>Développeur React Native - IA Assistant</span>
      </div>

      <h1 class="hero-title text-4xl md:text-5xl lg:text-6xl font-extrabold leading-tight mb-6 tracking-tight">
        Transformez vos idées en
        <span class="gradient-text">applications exceptionnelles</span>
      </h1>

      <p class="hero-description text-lg md:text-xl leading-relaxed mb-8 opacity-90 max-w-[700px] mx-auto">
        Développeur React Native spécialisé dans la création d'applications mobiles modernes.
        J'utilise l'intelligence artificielle pour optimiser chaque étape du développement et livrer
        des solutions de qualité professionnelle.
      </p>

      <div class="hero-stats grid grid-cols-1 md:grid-cols-3 gap-5 md:gap-8 mb-8 max-w-[600px] mx-auto">
        {stats.map(stat => (
          <div class="stat-item text-center p-3 md:p-0 bg-white/10 md:bg-transparent rounded-lg md:rounded-none backdrop-blur-lg md:backdrop-blur-none border border-white/20 md:border-none">
            <span class="stat-number block text-base md:text-lg font-bold mb-1">{stat.number}</span>
            <span class="stat-label text-sm opacity-80">{stat.label}</span>
          </div>
        ))}
      </div>

      <div class="hero-buttons flex flex-col md:flex-row gap-3 md:gap-4 justify-center items-center mb-12">
        <a href="#services" class="btn btn-primary w-full md:w-auto max-w-xs justify-center">
          <span>Découvrir mes services</span>
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="m9 18 6-6-6-6"/>
          </svg>
        </a>
        <a href="#contact" class="btn btn-secondary w-full md:w-auto max-w-xs justify-center">
          <span>Démarrer un projet</span>
        </a>
      </div>
    </div>
  </div>

  <div class="hero-scroll absolute bottom-8 left-1/2 -translate-x-1/2 z-10">
    <div class="scroll-indicator flex flex-col items-center gap-2 text-white/70 text-sm animate-bounce-slow">
      <span>Découvrir</span>
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="m6 9 6 6 6-6"/>
      </svg>
    </div>
  </div>
</section>

<style>
  .gradient-text {
    background: linear-gradient(135deg, #ffffff 0%, rgba(255, 255, 255, 0.8) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
</style>
